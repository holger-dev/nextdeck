# fastlane snapshot configuration for Nextdeck

scheme("Runner")

# iPhone only (for now)
devices([
  "iPhone 17 Pro",
])
ios_version("26.2")

languages([
  "en-US",
])

output_directory("../screenshots")

clear_previous_screenshots(true)
# Leave the app installed so demo data/logins stay intact between runs
reinstall_app(false)
erase_simulator(false)

skip_open_summary(true)
concurrent_simulators(false)
